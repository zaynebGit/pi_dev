<form method="post" action="{{ path('app_resp_quiz_delete', {'id': resp_quiz.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ resp_quiz.id) }}">
    <button class="btn">Delete</button>
</form>
